services:
  - type: web
    name: kali-linux-gui
    runtime: docker
    plan: starter
    envVars:
      - key: PORT
        value: 54321  # This tells Render to route traffic to this container port
